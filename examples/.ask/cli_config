{
  "profiles": {
    "default": {
      "token": {
        "access_token": "${access_token}",
        "refresh_token": "${refresh_token}",
        "token_type": "bearer",
        "expires_in": 3600,
        "expires_at": "${expires_at_timestamp}"
      },
      "vendor_id": "${vendor_id}"
    }
  }
}